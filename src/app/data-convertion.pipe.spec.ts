import { DataConvertionPipe } from './data-convertion.pipe';

describe('DataConvertionPipe', () => {
  it('create an instance', () => {
    const pipe = new DataConvertionPipe();
    expect(pipe).toBeTruthy();
  });
});
